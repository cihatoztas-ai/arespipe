-- Bu dosya kasten BOZUK isimle oluşturulmuştur (self-test için).
-- İsim formatı NNN_aciklama.sql değil → MIG_ISIM_BOZUK yakalanmalı.
-- Header yorumu VAR (bu satırlar) → MIG_HEADER_EKSIK bu dosyadan çıkmaz.
-- Baştaki 3 rakam yok, duplicate kontrolüne girmez.

BEGIN;
SELECT 1;
COMMIT;
